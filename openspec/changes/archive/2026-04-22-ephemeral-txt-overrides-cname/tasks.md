## 1. Root zone path

- [x] 1.1 Reorder ephemeral TXT lookup before exact CNAME fallback on root zone path in `internal/server/handler.go:handleRootQuery`: move the `lookupEphemeralTXT` call (currently at lines 201-206) to immediately after the `rootZone.Lookup(qname, qtype)` call at line 187-191 and before the CNAME fallback at lines 193-199; ensure the wildcard fallback at lines 208-219 remains after this reorder and the overall function return order stays: exact-type → ephemeral-TXT → exact-CNAME fallback → wildcard fallback
- [x] 1.2 Update the code comment block above `lookupEphemeralTXT` in `internal/server/handler.go` to cite both RFC 4592 §2.2.1 (existing wildcard rationale) and the new "Intentional RFC 1034 §3.6.2 deviation is scoped to TXT qtype" design decision; state explicitly that the overlay applies only when `qtype == TypeTXT` AND at least one live entry exists, and that `dig CNAME` / other qtypes are unaffected
- [x] 1.3 [P] Add a unit test `TestEphemeral_OverridesExactCNAME_RootZone` in `internal/server/handler_ephemeral_test.go` that builds a root zone with `_acme-challenge.foo.example.com. CNAME acme-dns.external.net.`, puts a TXT entry for the same qname into the ephemeral store, issues a TXT query, and asserts the answer contains exactly one TXT RR with the ephemeral value and no CNAME RR
- [x] 1.4 [P] Add a unit test `TestEphemeral_CNAMEQueryUnaffectedByEphemeralTXT` in `internal/server/handler_ephemeral_test.go` that uses the same zone + ephemeral setup as 1.3 but issues a CNAME query and asserts the response contains exactly the zone CNAME RR (no TXT)
- [x] 1.5 [P] Add a unit test `TestEphemeral_TXTQueryFallsBackToCNAMEWhenStoreEmpty` in `internal/server/handler_ephemeral_test.go` that puts no ephemeral entry, configures `_acme-challenge.foo.example.com. CNAME target.example.com.` and `target.example.com. TXT "zone-txt"`, issues a TXT query, and asserts the answer contains the CNAME followed by the `target.example.com. TXT "zone-txt"` record (standard RFC 1034 §3.6.2 behavior preserved)
- [x] 1.6 [P] Add a unit test `TestEphemeral_NonTXTQueryAtCNAMEUnaffected` in `internal/server/handler_ephemeral_test.go` that places an ephemeral TXT entry at `foo.example.com.`, configures `foo.example.com. CNAME target.example.com.` and `target.example.com. A 1.2.3.4`, issues an A query, and asserts the response follows the CNAME to the A record (ephemeral TXT not exposed)

## 2. Backup (alias) zone path

- [x] 2.1 Apply equivalent reorder on backup zone path in `internal/server/handler.go:handleBackupQuery`: move the `lookupEphemeralTXT` call (currently at lines 261-267) so that for `qtype == TypeTXT` the ephemeral check runs before `alias.ResolveExact` (lines 256-259) is allowed to perform its internal CNAME fallback, while keeping the wildcard resolution at `alias.ResolveWildcard` (lines 269-272) after this check
- [x] 2.2 Verify in `internal/alias/override.go` that `ResolveExact` (and `finalizeBackupRRs` with its RFC 1034 §3.6.2 CNAME fallback at line 54) does NOT require any change for this behaviour, because the handler-layer reorder in 2.1 keeps the ephemeral store coupling out of `internal/alias`; add a short package-level comment to `internal/alias/override.go` documenting that ephemeral overlay is intentionally handled in the server layer, not inside `alias`
- [x] 2.3 [P] Add a unit test `TestEphemeral_OverridesExactCNAME_BackupZone` in `internal/server/handler_ephemeral_test.go` that configures a backup alias (e.g., `backup.com.` aliased to `example.com.`), places a root-zone `_acme-challenge.foo.example.com. CNAME acme-dns.external.net.`, puts an ephemeral TXT entry keyed by the backup-namespace qname `_acme-challenge.foo.backup.com.`, issues a TXT query for `_acme-challenge.foo.backup.com.`, and asserts the answer contains exactly one TXT RR owned by the backup-namespace qname with no CNAME emitted

## 3. Integration coverage

- [x] 3.1 Add a new integration test file `test/integration/ephemeral_overrides_cname_test.go` exercising the end-to-end DNS path over UDP and TCP: set up a zone with an `_acme-challenge.*` exact CNAME to an out-of-bailiwick target, PUT a TXT entry via the in-process ephemeral API, run `dig TXT` and `dig CNAME` equivalents, and assert the TXT response contains only the ephemeral value while the CNAME response contains the zone CNAME
- [x] 3.2 [P] Extend `test/integration/cname_following_test.go` with a regression test confirming that when the ephemeral store has no entry (or is nil), exact-CNAME + TXT query behaviour is byte-for-byte identical to before this change, including the answer section order (CNAME first, target TXT second) and the AA flag
- [x] 3.3 [P] Confirm via existing tests that the original `TestEphemeral_ExactBeatsWildcardTXT`, `TestEphemeral_ExactBeatsWildcardCNAME`, and `TestEphemeral_WildcardStillAppliesWithoutExactMatch` in `internal/server/handler_ephemeral_test.go` still pass unchanged after the reorder (no modifications permitted to these three tests)

## 4. Verification and docs

- [x] 4.1 Run `make lint && make test` and ensure all unit tests pass, including the new TXT-overlay tests and the untouched wildcard regression tests (document the intentional RFC 1034 §3.6.2 deviation is scoped to TXT qtype in a code comment near the reordered block in `internal/server/handler.go`)
- [x] 4.2 Run `make test` with `-run "Integration|ephemeral_overrides_cname"` to ensure the new integration test passes over both UDP and TCP, and that existing integration tests in `test/integration/cname_following_test.go` and `test/integration/cname_synthesis_test.go` remain green
- [x] 4.3 Update `docs/ephemeral-api.md` (if present; otherwise add a new section to the nearest user-facing doc under `docs/`) to document the TXT-overrides-exact-CNAME behavior for the three Requirements added/modified by this change — Listen for DNS queries on UDP and TCP port 53, Synthesize CNAME response when qtype does not match but CNAME exists at the queried name, Ephemeral TXT entries override exact CNAME at the same qname for TXT queries — including the reverse note that `dig CNAME` and non-TXT qtypes are unaffected
- [x] 4.4 Build a local `.deb` via `make deb` using the `ephemeral-txt-overrides-cname` change name embedded in the version, deploy to `bench-ns2` via the `release-shadowdns` skill in local-change mode, and verify in `journalctl -u shadowdns` that the service starts cleanly after the reorder
