## 1. 基礎件：predicate 與 metrics 語意（internal/config + internal/metrics）

- [x] 1.1 [P] geo 規則掃描 predicate：依「geo 規則掃描 predicate 放在 internal/config 與規則型別同檔」決策，在 internal/config/match.go 新增掃描 `[]View` 的 predicate，回報第一個（依宣告順序）match-clients 含 `CountryRule` 或 `ASNRule` 的 view 的名稱、`View.Source`、`View.Line` 與是否存在；型別 switch 加註解要求新增 geo 類規則型別時同步檢視。先寫失敗測試再實作（tdd）：internal/config/match_test.go 覆蓋無 geo 規則、country 規則、ASN 規則、多 view 取第一個、空 view 清單五類案例，`go test ./internal/config/` 全綠。
- [x] 1.2 [P] SetGeoIPInfo 完整期望集合語意：依「SetGeoIPInfo 把傳入 map 視為完整期望集合」決策修改 internal/metrics/metrics.go——對 `prevGeoIPLabels` 中存在但本次 `buildEpochs` 缺席的 database 刪除其 series 並移出追蹤；滿足 spec「Expose GeoIP database metadata」的新行為（空 map 即清空所有 `shadowdns_geoip_db_info` series）。先寫失敗測試再實作：internal/metrics/metrics_test.go 覆蓋空 map 刪除全部 series、部分缺席只刪缺席者、既有同 database 換 build_time 行為不變三類案例，`go test ./internal/metrics/` 全綠。

## 2. 啟動與 reload 條件式 GeoIP 載入（cmd/shadowdns）

- [x] 2.1 啟動條件式載入：依「GeoIP 需求判定：directory 有設就載入，未設才掃描 geo 規則」決策，在 cmd/shadowdns 新增單一共用 helper（形如 `loadGeoIPIfRequired(cfg, logger)`，三分支邏輯與錯誤訊息組裝只存在這一處，供啟動與 reload 共用）並改寫 run() 的啟動驗證，實作 spec MODIFIED「Fail startup when GeoIP databases are missing or unreadable」——`geoip-directory` 有設（非空字串）一律 LoadGeoIP（失敗 fatal、候選鏈行為不變）；未設（缺席或空字串，行為相同）且存在 geo 規則則以 predicate 結果回致命錯誤（訊息含 view 名稱、source 路徑、行號）；未設且無 geo 規則則以 nil handle 繼續。先寫失敗測試再實作：cmd/shadowdns/main_test.go 覆蓋「無 geo 規則免 mmdb 啟動成功」、「有 geo 規則缺 directory 失敗且錯誤訊息含 source:line」、「`geoip-directory "";` 空字串與缺席行為相同」三類案例，`go test ./cmd/shadowdns/` 全綠。
- [x] 2.2 無 GeoIP 運作的可觀察面：實作 spec ADDED「Operate without GeoIP databases when configuration declares no geo rules」——nil handle 下 SetGeoIPInfo 呼叫端改傳空 map（不解參考 `Metadata()`）；依「啟動與 reload 摘要日誌補 geoip_enabled 欄位」決策在 shadowdns ready、reload complete 與 dry-run 摘要三條 Info 日誌補 `geoip_enabled` 布林欄位（dry-run 在 ready 日誌前提早返回，必須單獨補）；依「ECS 無 GeoIP 時啟動 Warn 不 fatal」決策在設定載入完成且結果為 `--ecs-enable` + 未載入 GeoIP 時輸出一條 Warn；`--dry-run` 與真啟動同成敗——無 geo 規則、無 mmdb 設定下成功，有 geo 規則缺 directory 時失敗且錯誤相同；同步修正 internal/view/matcher.go 的 Matcher 註解——nil GeoIP DB 在無 geo 規則設定下為合法生產狀態（非僅測試用）。驗證：cmd/shadowdns 測試斷言 ready 與 dry-run 日誌的 `geoip_enabled` 欄位、ECS Warn 出現、dry-run 成功與失敗兩個方向、metrics 端點無 `shadowdns_geoip_db_info` series，`go test ./cmd/shadowdns/` 全綠。
- [x] 2.3 reload 條件式載入與轉換態：依「啟動與 reload 共用同一條件式載入語意」決策讓 reload 路徑改呼叫 2.1 的共用 helper，實作 spec MODIFIED「GeoIP databases are reloaded on SIGHUP」——條件式判定與啟動一致；no-geo→geo 成功後查詢用新 mmdb、reload complete 日誌 `geoip_enabled=true`、mmdb 開啟失敗則 keep-old；geo→no-geo 成功後以 nil handle 服務、reload complete 日誌 `geoip_enabled=false`、被取代 handle 走既有 deferred-close 世代輪替、`shadowdns_geoip_db_info` series 全數刪除、若 `--ecs-enable` 啟用則該次 reload 輸出 ECS 無 GeoIP Warn；geo 規則缺 directory 的 reload 失敗 keep-old 且 `shadowdns_reload_total{result="failure"}` 遞增。先寫失敗測試再實作：cmd/shadowdns/main_reload_test.go 覆蓋上述三個轉換情境（含 reload 日誌欄位與 ECS Warn 斷言、連續 geo↔no-geo 切換一輪驗證 deferred-close 不 panic），`go test ./cmd/shadowdns/` 全綠。

## 3. 整合測試與回歸

- [x] 3.1 無 GeoIP 端到端整合測試：test/integration/helpers_test.go 的 fixture helper 現況是無條件產生 mmdb、呼叫 view.LoadGeoIP、且 named.conf fixture 寫死 geoip-directory 與 geo 規則 view——先提供跳過 GeoIP 的 helper 變體與無 geo 規則的 named.conf fixture，再新增 test/integration/geoip_optional_test.go：驗證只用 any/IP/CIDR 規則、未設 `geoip-directory`、tmp 目錄無任何 mmdb 的設定可啟動，且來源 IP 命中 CIDR 規則的 A 查詢取得權威回應（NOERROR + 正確 RR）——覆蓋 dry-run 測不到的 matcher 與查詢路徑；`go test ./test/integration/` 全綠（既有測試不受 helper 變體影響）。
- [x] 3.2 全套回歸：既有「geoip-directory 有設」設定的行為位元等價（既有測試零修改即通過），`make test`（race）、`make lint`、`make smoke` 全部通過。

## 4. 手冊與文件

- [x] 4.1 [P] GeoIP 與 ECS 手冊頁更新：docs/configuration/geoip.md 與 docs/configuration/geoip.zh.md 改寫需求說明為條件式（僅在使用 `geoip country`/`geoip asnum` 規則或設定 `geoip-directory` 時需要 mmdb；空字串與未設等同），補上「未載入時 metrics 無 `shadowdns_geoip_db_info` series」與「`--ecs-enable` 無 GeoIP 時 Warn」兩個可觀察差異；docs/configuration/named-conf.md 與 docs/configuration/named-conf.zh.md 的 `geoip-directory` 欄位說明同步改為「僅在使用 geo 規則時必填」；docs/guides/ecs.md 與 docs/guides/ecs.zh.md 補「無 GeoIP 時 ECS 對 view 選擇無作用（僅剩 option echo）＋啟動／reload Warn」一節（現行內容假設 GeoIP 恆載入）；範例僅用 RFC 2606 網域與 RFC 5737 IP，`make docs-build`（strict）通過。
- [x] 4.2 [P] 「GeoIP 必要」描述全面檢視：docs/reference/cli.md 與 docs/reference/cli.zh.md 的 SIGHUP 信號說明（「re-reads ... the GeoIP mmdb files」）與 `--named-conf` 描述改為條件式措辭；檢查 docs/getting-started.md、docs/getting-started.zh.md、docs/migration.md、docs/migration.zh.md、docs/index.md、docs/index.zh.md 與 README.md 是否有「GeoIP/mmdb 為啟動必要條件」的描述需要反映條件式需求；需要則更新（README 用英文、雙語檔一律成對改），不需要則在勾選本任務時明確記錄「已檢視、無需變更」的結論；`make docs-build` 通過。
