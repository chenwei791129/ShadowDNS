# Integration-test fixture. The aliases block uses the unified mapping form
# (members + optional rewrite_rdata_labels) — the only shape accepted by
# the loader.
aliases:
  example.com:
    members:
      - backup.example
  # Mapping form sample (commented; left here for documentation only — no
  # corresponding zone fixtures are loaded for this root):
  # templated-root.example:
  #   members:
  #     - templated-alias.example
  #   rewrite_rdata_labels: true
