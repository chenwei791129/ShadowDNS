// Debian-style viewless named.conf for the BIND-compatibility integration test.
// Mirrors the stock /etc/bind/named.conf layout: a thin top-level file that
// pulls in options, operator-local statements, and the default zones.
include "named.conf.options";
include "named.conf.local";
include "named.conf.default-zones";
